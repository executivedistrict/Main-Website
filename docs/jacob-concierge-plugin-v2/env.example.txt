# ─── Jacob Concierge — Required Environment Variables ─────────────────────────
# Copy this file to .env in your project root.
# These credentials are NEVER exposed to the browser.
# They are used only by the backend endpoint (server.js).

# Your Tavus API key (get from https://platform.tavus.io)
TAVUS_API_KEY=your_tavus_api_key_here

# Jacob's Replica ID (provided by Executive District)
TAVUS_REPLICA_ID=your_replica_id_here

# Jacob's Persona ID (provided by Executive District)
TAVUS_PERSONA_ID=your_persona_id_here

# ─── Production CORS (Required for production) ───────────────────────────────
# Comma-separated list of allowed origins. Do NOT use wildcard (*) in production.
ALLOWED_ORIGINS=https://executivedistrict.com,https://www.executivedistrict.com

# ─── Node Environment ────────────────────────────────────────────────────────
# Set to "production" for deployed sites. This enforces strict CORS and hides
# health endpoint details.
NODE_ENV=production
